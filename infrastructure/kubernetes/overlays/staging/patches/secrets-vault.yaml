# Placeholder — sera rempli à l'étape 6 (Secrets via Vault)
# Cible : remplacer les ExternalSecret/ClusterSecretStore AWS par
# Vault Agent Injector annotations sur les pods consommateurs.
#
# Ce fichier n'est PAS listé dans kustomization.yaml tant qu'il est vide.
